
{{ form_start(formSearch) }}
    <div class="row me-5">
        <div class="col-9">
            {{ form_widget(formSearch.search_value) }}
        </div>
        <div class="col-1">
            <button type="submit">
                <i class="fa-solid fa-magnifying-glass"></i>
            </button>
        </div>
    </div>
{{ form_end(formSearch) }}