{# <a href="{{path('app_user',{slug :app.user.slug})}}">{{ app.user.name ?? 'connexion' }}</a> #}


{% extends 'base-front.html.twig' %}

{% block body %}

<div class="container">
    <h1> {{pagination.getTotalItemCount}}</h1>
    {% for account in pagination %}
        <p>{{account.name}}</p>
    {% endfor %}

    <div>
        {{knp_pagination_render(pagination)}}
    </div>
</div>

{% endblock %}