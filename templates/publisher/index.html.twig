{% extends 'base.html.twig' %}

{% block title %}{{'home.menu.publishers'|trans}}{% endblock %}

{% block body %}

    <div class="container">
        <h2>{{'home.menu.publishers'|trans}}</h2>
        <div class="row mx-auto">
            <div class="col-lg-1">
            </div>
            <div class="col-lg-4">
                <h3>{{'home.menu.name'|trans}}</h3>
            </div>
            <div class="col-lg-4">
                <h3>{{'home.menu.website'|trans}}</h3>
            </div>
            <div class="col-lg-3">
                <h3>{{'home.menu.nbGame'|trans}}</h3>
            </div>
        </div>
        {% for publisher in publishers %}
            <div class="row">
                <div class="col-lg-1">
                    <img src="{{ publisher.country.urlFlag }}">
                </div>
                <div class="col-lg-4">
                    <a href="{{path('app_publisher_show', {slug:publisher.slug}) }}">{{ publisher.name }}</a>
                </div>
                <div class="col-lg-4">
                    <a href="{{publisher.website}}" target="_blank">{{ publisher.website }}</a>
                </div>
                <div class="col-lg-3">
                    {{ publisher.games|length }}
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}
